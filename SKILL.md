---
name: agent-relay
description: 跨 Agent 的个人开发上下文层（AI 项目接力框架）——个人开发者预算有限，免费额度用完就换 AI 工具接力开发同一项目，本框架让任何 Agent 接入后 5 分钟理解项目现状、用户画像与工作规矩，做到"换一个 AI，感觉还是同一个开发助手"。当用户说"接手项目 / 开工 / 继续推进 / 收尾 / 交接给下一个 Agent / 汇报项目状态"，或项目根目录存在 AGENTS.md / PROJECT_CONTEXT.md / HANDOFF.md / USER_PROFILE.md / WORKFLOW.md 文档体系，或用户想为长期维护的项目建立这套可接力文档时使用。核心原则：项目状态与用户画像的唯一载体是文档，不是任何一次会话。
---

# Agent Relay —— 跨 Agent 的个人开发上下文层

> 解决的问题：个人开发者在多个 AI Coding 工具之间接力开发同一个项目（预算有限，一个平台免费额度用完就换下一个，如 ZCode → Codex → Claude → Gemini）。每换一次工具，新 Agent 都不了解项目历史、设计决策、用户的开发习惯和审美标准，需要重新培养。
> 目标体验：**换一个 AI，感觉还是同一个开发助手。**
> 定位：任何 Agent 只是执行者；Relay 提供统一的上下文与工作协议。不做多 Agent 编排 / 通信 / 任务调度，不绑定任何具体 AI 工具。

## 核心原则（三条）

1. **项目状态的唯一载体是文档，不是任何一次会话。** 会话是临时容器；文档没写 = 工作没做完。
2. **用户也是一份上下文。** 用户的画像与规矩同样落在文档（`USER_PROFILE.md` / `WORKFLOW.md`）：Agent 按文档适应用户，不是用户适应 Agent。
3. **协议随项目走，不随工具走。** 协议写在项目文件里，任何工具进来读同一套文件、按同一套流程干活——这就是"换 AI 不换体验"的机制。

## 文档体系（9 份）与分工

| 文件 | 回答的问题 | 性质 / 更新频率 |
|---|---|---|
| `AGENTS.md` | 入口：必读顺序、项目特有规则、红线、任务手册 | 很少变 |
| `USER_PROFILE.md` | 用户是谁、怎么开发、喜欢什么、讨厌什么 | 用户人格，跨项目基本一致 |
| `WORKFLOW.md` | Agent 的标准工作协议（开工 / 执行 / 收尾 / 部署边界） | 很少变 |
| `PROJECT_CONTEXT.md` | ⭐ 项目现在什么状态 | 最常更新（收尾必刷） |
| `HANDOFF.md` | 上一任完成了什么、留了什么坑、下一任继续什么 | 每次换班追加 |
| `ROADMAP.md` | 接下来往哪走 | 阶段推进时更新 |
| `DECISIONS.md` | 为什么这样设计 | 有新决策时追加 |
| `CHANGELOG.md` | 改过什么、谁改的（必填工具+模型） | 每次收尾追加 |
| `CLAUDE.md` | 指针文件（兼容只认 CLAUDE.md 的工具） | 极少变 |

**职责边界**（避免同一信息记两处）：

- `PROJECT_CONTEXT` 记事实快照（现在时）；`HANDOFF` 记给下一任的话（换班时刻）；`CHANGELOG` 记每次改动的存档
- `WORKFLOW` 记流程（怎么做）；`DECISIONS` 记理由（为什么）；`AGENTS` 记项目特有规则
- 用户的长期画像只放 `USER_PROFILE`，不散落到 PROJECT_CONTEXT

## 第一步：判断项目是否已接入本框架

检查项目根目录是否存在 `AGENTS.md` 与 `PROJECT_CONTEXT.md`：

- **存在** → 项目已接入，进入【开工流程】；若缺 v2 新增的三份文件（USER_PROFILE / WORKFLOW / HANDOFF），按【从 v1 升级】补齐
- **不存在** → 向用户确认是否初始化；确认后复制 `assets/templates/` 中的 9 份模板到项目根目录，按项目实际逐份填写（缺真实信息用占位符标 TODO，**不编造**），然后按【收尾流程】提交

### 初始化规则

- `USER_PROFILE.md` 特殊：**必须逐项询问用户本人**（技术水平 / 开发方式 / 审美好恶 / 沟通习惯 / 协作红线），用户没说就留 TODO，严禁脑补；用户在其他项目已有画像时，取来复制并请用户过目确认
- `WORKFLOW.md` 使用默认协议即可开工，项目特有约定写进 `AGENTS.md`
- `HANDOFF.md` 建立空记录即可；`PROJECT_CONTEXT.md` 按项目实际填写

### 从 v1 升级（项目里已有旧版 6 件套）

1. 补 3 份新文件：`USER_PROFILE.md`（逐项问用户）、`WORKFLOW.md`（用默认协议）、`HANDOFF.md`（把 PROJECT_CONTEXT 的"下一步"与 CHANGELOG 最近一条整理成第一条交接记录）
2. `PROJECT_CONTEXT.md`：原「关键上下文（用户偏好与约定）」迁移到 `USER_PROFILE.md`；补「项目档案」（目标 / 技术栈 / 当前版本 / 当前阶段）
3. `AGENTS.md`：更新必读顺序表；流程章节改为指向 `WORKFLOW.md`
4. `ROADMAP.md` / `DECISIONS.md` / `CHANGELOG.md` / `CLAUDE.md` 内容不动，仅对齐新模板的说明头

## 开工流程（新会话 / 新 Agent 接手）

按顺序读 4 份核心文件 + 3 份按需文件，明确"要从每个文件提取什么"：

| 顺序 | 文件 | 你要提取的答案 |
|---|---|---|
| 1 | `PROJECT_CONTEXT.md` | ⭐ 项目目标、技术栈、当前版本与阶段、已完成 / 未完成、当前问题 |
| 2 | `HANDOFF.md`（最新一条） | 上一任完成了什么、未解决什么、我应该从哪继续 |
| 3 | `USER_PROFILE.md` | 用户怎么开发、喜欢什么、讨厌什么、协作红线 |
| 4 | `WORKFLOW.md` | 本项目要求的工作协议 |
| 5 | `ROADMAP.md` | 当前阶段、剩余未完成项 |
| 6 | `DECISIONS.md` | 哪些方案已定（不要重新讨论）、为什么这么定 |
| 7 | `CHANGELOG.md`（最近几条） | 最近改了什么、由哪些 Agent 执行、上次"下一步"兑现了没有 |

然后向用户汇报**开工三件套**：① 当前状态 ② 已完成内容 ③ 下一步建议——先总结再动手，等用户确认或给出指令后再改代码。
若用户本次指令与 `PROJECT_CONTEXT.md` 的"下一步"冲突：先说明差异，经用户确认再动手。
同时按 `USER_PROFILE.md` 调整自己的行为方式：沟通语言与详略、方案粒度、审美取向都跟着用户走。

## 工作中守则（红线速览）

1. 不随意改变架构、不删除已有功能、优先小范围修改、保持已有设计语言（详见 WORKFLOW.md）
2. **不主动部署、不主动发布**，除非用户明确要求
3. 项目若约定了内容与代码分离（如 `data/` 目录为唯一内容源），严格遵守，不把内容硬编码
4. 不重命名、不删除核心治理文档；不推翻 `DECISIONS.md` 已采纳的决策，有异议就追加新条目说明理由
5. 用户信息 / 画像缺失时保留占位符并标 TODO，严禁编造
6. 上下文被压缩、会话中断或自己"失忆"：立即重读必读文件，一切以文档为准，禁止凭残存记忆继续操作

## 收尾流程（交棒，缺一不可）

1. 更新 `PROJECT_CONTEXT.md`：最后更新时间、**最近执行 Agent**、当前版本与阶段、一句话现状、未完成、当前问题
2. `HANDOFF.md` 顶部追加交接条目：交班 Agent、本次完成、当前状态、**未解决**、下一任应该继续
3. `CHANGELOG.md` 顶部追加条目，**必含执行 Agent**：

   ```markdown
   ## YYYY-MM-DD · 变更标题
   - **执行 Agent**：工具名（模型名），如 ZCode（GLM-5.3-Flash）
   - **修改内容**：……
   - **原因**：……
   - **影响**：……
   - **下一步**：……
   ```

4. 有新的重要选择 → `DECISIONS.md` 追加新编号（不改旧结论，废弃才标记）
5. 阶段推进 → 更新 `ROADMAP.md` 勾选项与状态
6. **git 提交并推送**（项目使用 git 时）：推送位置遵守「远端管理」的询问与记录规则；远端是代码与文档的唯一备份；配置了自动部署（如 Vercel）时推送即上线
7. **镜像同步**（若采用项目主文件夹模式）：把 workspace 中更新过的治理文档复制回主文件夹根
8. **workspace 回收**：按「项目主文件夹与工作循环」弹窗确认——用户同意才删，保留则原样不动
9. 向用户输出**汇报四件套**：修改内容 / 修改原因 / 测试方式 / 下一步建议；最后确认："**本会话工作已全部落档，可以安全结束会话。**"

## 用户开场白模板（写给用户复制）

- 首次接手：`请阅读 AGENTS.md 接手本项目，先汇报当前状态，再等我指令。`
- 继续推进：`请阅读 AGENTS.md，按 PROJECT_CONTEXT 和 HANDOFF 的交接继续推进。`

## 新机器准备（首次在这台电脑上工作本项目）

1. 安装项目所需工具链（以项目 README 为准，如 Node.js、git、语言运行时）
2. 完成代码托管平台认证（如 `gh auth login`，认证必须由用户本人完成）
3. 克隆仓库 → 安装依赖 → 按项目说明本地运行
4. 配置了自动部署的项目：push 即部署，无需本地构建发布
5. 密钥与凭证只存本机钥匙串或 `.env`（已在 .gitignore），永不提交入库

## 部署类项目的接力模型（网站/App 等）

**工作永远发生在本地克隆里**；链条是：

```text
本地文件夹（工作区）→ git commit → git push → 远端仓库（唯一真相/备份）→ 自动部署 → 线上更新
```

1. **部署不是需要"接管"的对象，而是 push 的自动结果**——Agent 日常不登录部署平台
2. 线上事实（部署地址、平台、关键设置如 Root Directory、域名状态）必须记录在 `PROJECT_CONTEXT.md` 的「环境与发布」，Agent 只读文档即可知道线上长什么样、怎么更新
3. 平台账号类操作（Vercel/域名/服务器控制台、绑域名、改环境变量）属于**用户本人的一次性操作**；Agent 的职责是把需要用户操作的步骤写清楚，而不是索要账号
4. 换电脑接力：认证 → 克隆 → 读文档 → 本地干活 → push；本地未推送的内容对其他机器不存在，所以**收尾必 push**
5. 线上验证是收尾的一部分：push 后用文档里记录的线上地址做一次冒烟检查

## 远端管理：上传位置与确认机制

远端仓库不是默认存在的——**上传到哪里，永远先问用户，再记录**。适用于任何托管平台（GitHub / GitLab / 自托管等）。

1. **首次上传**：项目没有 git remote 时，询问用户选择：① 新建远端仓库 ② 使用用户已有的仓库地址 ③ 暂不上传（仅本地提交）
2. **记录位置**：推送成功后，把远端地址写入 `PROJECT_CONTEXT.md` 的「环境与发布」——这就是"上次上传的位置"
3. **后续推送**：默认推到 PROJECT_CONTEXT 记录的位置；若记录缺失、或与实际 remote 不一致，**先询问**：继续上次的位置，还是换新位置？用户确认换新后，同步更新 PROJECT_CONTEXT
4. **换平台**（如 GitHub → GitLab / 自托管）：同样先询问，用户确认后再迁移并更新记录
5. 隐私提醒：公开远端前，确认仓库内（含提交历史与作者信息）不含用户不愿公开的个人信息

## 项目主文件夹与工作循环（本地空间回收）

**标准循环**：进入项目主文件夹（读镜像治理文档）→ clone 远端源码到主文件夹的 `workspace/` 子目录 → 优化 → push 上线 → 把更新后的治理文档同步回主文件夹 → 弹窗确认后删除 `workspace/` → 主文件夹回到稳定态。

每个项目在本地有一个**固定的项目主文件夹**——它是项目的"家"，永远存在、永远可被发现，Agent 被放进这个文件夹就能接手：

```text
<项目主文件夹>/
├── AGENTS.md  PROJECT_CONTEXT.md  HANDOFF.md  USER_PROFILE.md  WORKFLOW.md
├── ROADMAP.md  DECISIONS.md  CHANGELOG.md  …                        # 治理文档镜像
└── workspace/    # 仅优化周期存在：远端仓库的完整 clone（源码 + 仓库内最新治理文档）
```

1. **主文件夹里的治理文档是镜像**，供人和 Agent 随时快速了解项目；**真相源永远是远端仓库与 workspace 内的文档**——两者冲突时以仓库为准
2. **开工**：在主文件夹内 `git clone <远端> workspace`（workspace 已存在则 `git -C workspace pull` 更新）→ 读 workspace 内最新的 AGENTS.md / PROJECT_CONTEXT.md / HANDOFF.md → 按开工流程干活
3. **收尾**（push 完成后）：① **镜像同步**——把 workspace 中更新过的治理文档复制回主文件夹根（收尾清单必做项）② 弹窗确认后删除 workspace 子目录
4. **删除确认硬门槛**：workspace 的删除同样必须交互式弹窗（口头提及不豁免），优先移入废纸篓
5. **多设备**：主文件夹可整体打包（zip）拷贝到其他设备，新设备上的 Agent 流程完全相同
6. 好处：项目永远在同一位置，Agent 不会"找不到项目"；源码在周期之间不占本地空间

**上传 ≠ 授权删除**。是否回收 workspace，必须单独弹窗询问——有人只想上传、有人想释放空间，不要替用户决定。

**再次接手：**

1. 进入项目主文件夹 → 读镜像文档了解项目 → clone/pull workspace → 按 workspace 内最新文档开工。没有主文件夹也没关系：托管平台仓库列表里能找到，clone 后为其补建主文件夹
2. 注意：`.env` 与钥匙串凭证不入库，重新接手需按项目文档重建；`node_modules`、构建产物可再生

## 参考实现

<!-- 隐私提示：参考项目如涉及隐私，只写"已落地"不写具体地址；项目细节记录在使用者自己的项目文档里 -->

本框架已在真实项目完整落地：个人网站（内容与代码分离、双语回退、治理文档、push 触发自动部署的完整链路）。落地细节请参考使用者各自项目内的治理文档。
