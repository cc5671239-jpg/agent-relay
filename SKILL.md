---
name: agent-relay
description: AI 项目接力框架——长期维护项目的开工、收尾与换班协议。当用户说"接手项目 / 开工 / 继续推进 / 收尾 / 交接给下一个 Agent / 汇报项目状态"，或项目根目录存在 AGENTS.md / PROJECT_CONTEXT.md / ROADMAP.md / DECISIONS.md / CHANGELOG.md 文档体系，或用户想为一个长期维护的项目建立这套可接力文档时使用。核心原则：项目状态的唯一载体是文档，不是任何一次会话。
---

# Agent Relay —— AI 项目接力框架

> 让任何一个新 Agent 在 5 分钟内接手项目，让任何一个老 Agent 完整交棒。
> 适用于多工具 / 多会话 / 跨机器接力维护的任何项目（网站、应用、内容库等）。

## 核心原则（先记住这一句）

**项目状态的唯一载体是文档，不是任何一次会话。** 会话是临时容器；文档没写 = 工作没做完。

## 第一步：判断项目是否已接入本框架

检查项目根目录是否存在 `AGENTS.md` 与 `PROJECT_CONTEXT.md`：

- **存在** → 项目已接入，直接进入【开工流程】
- **不存在** → 向用户确认是否初始化；确认后复制 `assets/templates/` 中的模板到项目根目录，按项目实际逐份填写（缺真实信息用占位符标 TODO，**不编造**），然后按【收尾流程】提交

## 开工流程（新会话）

按顺序读 5 个文件，并明确"要从每个文件提取什么"：

| 顺序 | 文件 | 你要提取的答案 |
|---|---|---|
| 1 | `README.md` | 项目是什么、文档地图 |
| 2 | `PROJECT_CONTEXT.md` | ⭐ 现在到哪一步、上次做到哪、下一步、风险 |
| 3 | `ROADMAP.md` | 当前阶段、剩余未完成项 |
| 4 | `DECISIONS.md` | 哪些方案已定（不要重新讨论）、为什么这么定 |
| 5 | `CHANGELOG.md` | 最近改了什么、由哪些 Agent 执行、上次"下一步"兑现了没有 |

然后向用户汇报**开工三件套**：① 当前状态 ② 已完成内容 ③ 下一步建议，随后等指令。
若用户本次指令与 `PROJECT_CONTEXT.md` 的"下一步"冲突：先说明差异，经用户确认再动手。

## 工作中守则（红线速览）

1. 项目若约定了内容与代码分离（如 `data/` 目录为唯一内容源），严格遵守，不把内容硬编码
2. 不重命名、不删除核心治理文档
3. 不推翻 `DECISIONS.md` 已采纳的决策；有异议就追加新决策条目说明理由
4. 用户真实信息缺失时保留占位符并标 TODO，严禁编造
5. 上下文被压缩、会话中断或自己"失忆"：立即重读 5 个文件，一切以文档为准，禁止凭残存记忆继续操作

## 收尾流程（交棒，缺一不可）

1. 更新 `PROJECT_CONTEXT.md`：最后更新时间、**最近执行 Agent**、一句话现状、下一步、风险
2. `CHANGELOG.md` 顶部追加条目，**必含执行 Agent**：

   ```markdown
   ## YYYY-MM-DD · 变更标题
   - **执行 Agent**：工具名（模型名），如 ZCode（GLM-5.3-Flash）
   - **修改内容**：……
   - **原因**：……
   - **影响**：……
   - **下一步**：……
   ```

3. 有新的重要选择 → `DECISIONS.md` 追加新编号（不改旧结论，废弃才标记）
4. 阶段推进 → 更新 `ROADMAP.md` 勾选项与状态
5. **git 提交并推送**（项目使用 git 时）：

   ```bash
   git add -A && git commit -m "<类型>: <一句话变更>" && git push
   ```

   远端是代码与文档的唯一备份；配置了自动部署（如 Vercel）时推送即上线
6. 向用户确认："**本会话工作已全部落档，可以安全结束会话。**"

## 用户开场白模板（写给用户复制）

- 首次接手：`请阅读 AGENTS.md 接手本项目，先汇报当前状态，再等我指令。`
- 继续推进：`请阅读 AGENTS.md，按 PROJECT_CONTEXT 的"下一步"继续推进。`

## 新机器准备（首次在这台电脑上工作本项目）

1. 安装项目所需工具链（以项目 README 为准，如 Node.js、git、语言运行时）
2. 完成代码托管平台认证（如 `gh auth login`，认证必须由用户本人完成）
3. 克隆仓库 → 安装依赖 → 按项目说明本地运行
4. 配置了自动部署的项目：push 即部署，无需本地构建发布
5. 密钥与凭证只存本机钥匙串或 `.env`（已在 .gitignore），永不提交入库

## 部署类项目的接力模型（网站/App 等）

**工作永远发生在本地克隆里**；链条是：

```text
本地文件夹（工作区）→ git commit → git push → 远端仓库（唯一真相/备份）→ 自动部署 → 线上更新
```

1. **部署不是需要"接管"的对象，而是 push 的自动结果**——Agent 日常不登录部署平台
2. 线上事实（部署地址、平台、关键设置如 Root Directory、域名状态）必须记录在 `PROJECT_CONTEXT.md`，Agent 只读文档即可知道线上长什么样、怎么更新
3. 平台账号类操作（Vercel/域名/服务器控制台、绑域名、改环境变量）属于**用户本人的一次性操作**；Agent 的职责是把需要用户操作的步骤写清楚，而不是索要账号
4. 换电脑接力：认证 → 克隆 → 读文档 → 本地干活 → push；本地未推送的内容对其他机器不存在，所以**收尾必 push**
5. 线上验证是收尾的一部分：push 后用文档里记录的线上地址做一次冒烟检查

## 参考实现

<!-- 隐私提示：参考项目如涉及隐私，只写"已落地"不写具体地址；项目细节记录在使用者自己的项目文档里 -->

本框架已在真实项目完整落地：个人网站（内容与代码分离、双语回退、治理文档、push 触发自动部署的完整链路）。落地细节请参考使用者各自项目内的治理文档。
